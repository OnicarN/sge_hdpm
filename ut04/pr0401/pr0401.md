# EJERCICIOS
Lectura de un número válido
Crea un programa que solicite un número por pantalla 
al usuario y siga pidiéndolo hasta que el usuario
 introduzca un número válido.



```python
 a = input("Por favor introduzca un número por pantalla!!: ")

while(not a.isdigit()):
    print(" INTRODUCE UN NÚMERO >:( )")
    a = input ("Sigue introducindo números por favor ")

print("Muy biennn programa termiando!")
```







En este ejercicio se piden dos número para poder multiplicarlos entre si, los cuales van a estar almacenados en las variables n y k

```python
print ("Por favor introduce dos números, para poder multiplicarlos entre si ")

n = input("Primer número: ")
print("")
k = input("Segundo Número: ")

i = 1

while(i <= int(k)):
    resultado = int(n) * i
    print(n + "*"+str(i)+"="+ str(resultado))
    i+=1
```


Triángulo de asteriscos
Crea un programa que solicite un número al usuario 
y dibuje un triángulo con asteriscos cuya base sea 
el número introducido.

```python
a = input("Por favor introduce un número: ")

numero = int(a)

i = 1
estrella = "*"
while(i <= numero):
    print(estrella)
    estrella += "*"
    i+=1

```


Modifica el programa anterior para que en lugar de crear un triángulo
cree una pirámide. Si el usuario introduce un número par se lo volverá a pedir
hasta que introduzca un número par.

```python 
a = int(input("Introduce por favor un número impar : "))

estrella = "*"
iniciar = True
while(a % 2 == 0):
    print("muy mal")
    a = int(input("Por favor le pedimos que introduzca un número válido: "))
    
while(iniciar):  
        if(len(estrella) <= a):
            print(estrella.center(20))
            estrella += "**"
        else:
            iniciar = False

```

Número mayor y menor
Crea un programa que pida al usuario que introduzca 5 números
y luego le diga cuál es el mayor y el menor de todos ellos de la forma:
El número mayor es <mayor> y el menor es <menor>

```python
print("Introduce 5 números")
numero1 = int(input("Primer Número: "))
numero2 = int(input("Segundo Número: "))
numero3 = int(input("Tercer Número:"))
numero4 = int(input("Cuarto Número: "))
numero5 = int(input("Quinto Número: "))

numeros = []
numeros.append(numero1)   
numeros.append(numero2)   
numeros.append(numero3)   
numeros.append(numero4)   
numeros.append(numero5)   


numeros.sort()
print(numeros)


print("El número mayor es "+"<"+str(numeros[len(numeros)-1])+">"+" y el menor es "+"<"+str(numeros[0])+">")

```

Acierta el número
Crea un programa que genere un número
aleatorio entre 0 y 100 y el usuario tenga que adivinarlo.
Cada vez que el usuario introduzca un número el programa 
le dirá si el número es más alto o más bajo.

Para generar un número aleatorio puedes utilizar 
la función randint(a, b) que devuelve un entero 
aleatorio entre a y b. Para poder utilizar esta 
función antes tienes que importar la librería 
con la orden from random import *


```python
import random

numeroRandom = random.randint(0,100)

numeroUsuario = int(input("Por favor introduce un número entre el 0 y el 100: "))

def antiCheat(n):
    while (n < 0 or n > 100 ):
        print("Ese no es un número válido elige uno correcto")
        n = int(input("Elige otro numero : "))
    return n

def sigueEligiendo (n):
    if (n != numeroRandom):
        n = int(input("sigue eligiendo: "))
    return n

while(numeroUsuario != numeroRandom):
    numeroUsuario = antiCheat(numeroUsuario)
    if (numeroUsuario > numeroRandom):
        print("Tu número es mayor que el mio ")   
    else:
        print("Tu numero es menor que el mio ")
    numeroUsuario = sigueEligiendo(numeroUsuario)
print("HACERTASTE!!")
```


Piedra, papel o tijeras
Crea un programa que implemente el clásico juego de piedra, papel, tijeras, lagarto y spock.

Las normas de este juego son:

Piedra aplasta a lagarto
Lagarto envenena a Spock
Spock rompe las tijeras
Tijeras decapitan al lagarto
Lagarto come al papel
Papek desautoriza a Spock
Spock vaporiza la piedra
Piedra rompe a tijeras
Tijeras cortan papel
Papel envuelve a piedra
En el enlace anterior puedes ver un diagrama donde tal vez veas mejor las relaciones entre las diferentes elecciones.

Ganará el primero que gane 5 manos.




``` python
import random


posiblesOpciones = ["piedra","papel","tijera","lagarto","spock"]
numeroPartidas = int(input("Elige el número de partidas que quieres hacer: "))

puntosJugador = 0
puntosEnemigo = 0


i = 0
while (i < numeroPartidas):
    opcionMaquina = random.choice(posiblesOpciones)

    opcionJugador = input("Elige una de estas opciones: piedra,papel,tijera,lagarto,spock ")
    #en este caso lo que hago es que el enemigo elija una opción random de la lista

    if (opcionJugador == opcionMaquina):
        print("empate")
        print(opcionMaquina)
        puntosJugador += 1
        puntosEnemigo += 1
    else:
        match opcionJugador:
            case 'piedra' if opcionMaquina in ("tijera","lagarto"):
                print(opcionJugador+" gana a "+opcionMaquina)
                puntosJugador += 1
            case 'papel' if opcionMaquina in ("piedra","spock"):
                print(opcionJugador+" gana a "+opcionMaquina)
                puntosJugador += 1
            case 'tijera' if opcionMaquina in ("papel","lagarto"):
                print(opcionJugador+" gana a "+opcionMaquina)
                puntosJugador += 1
            case 'lagarto' if opcionMaquina in ("spock","papel"):
                print(opcionJugador+" gana a "+opcionMaquina)
                puntosJugador += 1
            case 'spock' if opcionMaquina in ("piedra","tijera"):
                print(opcionJugador+" gana a "+opcionMaquina)
                puntosJugador += 1
            case _:
                print(opcionMaquina+" gana a "+opcionJugador)
                puntosEnemigo += 1
    i+=1

print("----------------------------------")
print("PUNTOS JUGADOR: "+str(puntosJugador))
print("-----------------------------")
print("PUNTOS BOT: "+str(puntosEnemigo))

```

9.- Secuencia de Fibonacci
Crea un programa que genere los primeros n números de la secuencia de Fibonacci. 
Recuerda que la secuencia de Fibonacci es:

fib(0) = 1
fib(1) = 1
fib(n) = fib(n-2) + fib(n-1)


``` python
n = int(input("números de la secuencia de Fibonacci: "))
fibonacci = []

i = 0

if (n == 1):
    fibonacci.append(0)
    print(fibonacci)

else:
    fibonacci.append(0)
    fibonacci.append(1)
    while(i < n - 2):
        resultado = fibonacci[-1] + fibonacci[-2]
        fibonacci.append(resultado)
        i+=1
    print(fibonacci)
```

Números primos
Crea un programa que solicite un número al usuario y le diga si es primo o no. 
Un número primo solo es divisible por 1 y por sí mismo.

``` python
n = int(input("Por favor introduce un numero primo: "))

i = 1

numeroDivisores = 0

buscar = True
if (n == 1):
    print(str(n) + " no es un número primo") 
else:
    while (i < n + 1):
        if (n % i == 0):
            numeroDivisores += 1
        i+=1

    if (numeroDivisores == 2):
        print (f"{n} es un número primo")
    else:
        print(f"{n} no es un número primo")

```

En este ejercicio vamos a calcular el factorial el cual será introducido por el usuario por la consola

``` python
n = int(input("Por favor introduce un número para calcular el factorial: "))

i = n - 1
while(i > 0):
     n *=  iv
     i -=  1
print("el resultado es: "+ str(n))
```


Contador de vocales y consonantes
Crea un programa que solicite al usuario
una cadena de texto y muestre cuántas
vocales y cuántas consonantes contiene.


``` python
 cadena = input("Introduce por favor una cadena de texto: ")
 contadorVocales = 0
 contadorMayusc = 0

 i = 0
 while(i < len(cadena)):
     resultado = cadena[i]

     if (resultado.isupper()):
         contadorMayusc += 1
     elif (resultado.islower()):
         contadorVocales += 1
   
     i+=1

 print(f" hemos encontrado {contadorVocales} letras minúsculas y {contadorMayusc} letras mayúsculas en la palabra {cadena}")
 ```


Invertir una cadena
Crea un programa que pida al usuario una cadena de texto y muestre la misma cadena invertida.

Ejemplo: "python" → "nohtyp".


``` python
 cadena = input("Introduce una palabra para poder invertirla: ")

 letras = []

 i = 1
 while(i < len(cadena) + 1):
     letras.append(cadena[-i])
     i += 1

 ind = 0
 resultado = ""
 for i in letras:
    resultado += i

 print(resultado)
```

Palíndromos
Crea un programa que solicite al usuario una palabra y determine si es un palíndromo (se lee igual al derecho y al revés).

Ejemplo: "oso" → palíndromo.
Ejemplo: "casa" → no es palíndromo.


``` python
palabra = input("Por favor introduce una palabra: ")
 palabraReverse = ""
 i = 1
 while (i < len(palabra) + 1):
     palabraReverse += palabra[-i]
     i+=1

 if (palabra == palabraReverse):
     print(f"{palabra} es palíndromo")
 else:
     print(f"{palabra} no es palíndromo")
```
Números pares e impares
Crea un programa que pida al usuario un número entero 
positivo n y muestre todos los números pares e impares entre 1 y n.

```python
 n = int(input("Por favor introduce un número entero: "))

 for numero in range(1,n + 1):
     if (numero % 2 == 0):
         print (f"{numero} es un número par")
     else:
         print(f"{numero} es un número impar")
   
``` 

Suma de dígitos
Crea un programa que pida al usuario un número entero
y calcule la suma de todos sus dígitos.

Ejemplo: 327 → 3 + 2 + 7 = 12.
'''

``` python
n =  input("Por favor introduce un número entero: ")
digitos = []
i = 0
sigonoMas = "+"
resultadoNumerico = 0
resultadoFinal = ""
while(i < len(n)):
    digito = int(n[i])
    digitos.append(digito)
    i+=1
for numero in digitos:
    resultadoNumerico += numero

for numeroCaracter in digitos:
    if (not numeroCaracter == digitos[-1]):
        resultadoFinal += str(numeroCaracter) + " "+ sigonoMas + " "
    else:
        resultadoFinal += str(numeroCaracter) + " = " + str(resultadoNumerico)

print(resultadoFinal)
```


Calculadora simple
Crea un programa que pida al usuario dos números y
una operación (suma, resta, multiplicación o división) y muestre el resultado.

``` python
numeroA = int(input("Introduce el primer número por favor: "))
numeroB = int(input("Introduce el segundo número por favor: "))

opcionUsuario = input("Elija una opción: sumar, restar , multiplicar o dividir: ")

resultado = 0


continuar = True

while(continuar):
    match opcionUsuario:
        case "sumar":
            resultado = numeroA + numeroB
            continuar = False
        case "restar":
            resultado = numeroA - numeroB
            continuar = False
        case "multiplicar":
            resultado = numeroA * numeroB
            continuar = False
        case "dividir":
            resultado = numeroA / numeroB
            continuar = False
        case _:
            print("Esa no es una opción valida!")
            opcionUsuario = input("Elija otra vez: ")
        
print(resultado)

```

Ordenar lista de números
Crea un programa que pida al usuario 
una lista de números separados por comas y muestre la lista ordenada de menor a mayor.
'''
``` python 
n = input("Por favor introduce una lista de números separadas por comas: ")

separacion = n.split(",") 

numeros = []
for numero in separacion:
    numeros.append(int(numero))

def ordernarLista(numeros):
    for i in range(len(numeros)-1):
        for j in range(len(numeros)-1-i):
            if numeros[j] > numeros[j + 1]:
                aux = numeros[j]
                numeros[j] = numeros[j + 1]
                numeros[j + 1] = aux
    return numeros


print (ordernarLista(numeros))
```

[VOLVER A LA PÁGINA ANTERIOR](../index.md)